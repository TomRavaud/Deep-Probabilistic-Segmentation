learning_rate_monitor:
  _target_: lightning.pytorch.callbacks.LearningRateMonitor
  logging_interval: "epoch"  # "step" or "epoch"
  log_momentum: False  # whether to log the momentum of the optimizer